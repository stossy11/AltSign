on:
  push:
    branches:
    - master
name: Build
jobs:
  test:
    name: Build
    runs-on: macos-latest
    steps:
    - name: Checkout
      uses: actions/checkout@master
    - name: Test
      run: |
        swift build
